import { PositiveNumbersDirective } from './positive-numbers.directive';

describe('PositiveNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
